@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

    @if (SignInManager.IsSignedIn(User))
    {
        <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Account"><i class="fa fa-user"></i></a>
        <a asp-area="Identity" asp-page="/Account/Logout" title="Logout">Signout</a>
            @*<button type="submit" class="nav-link btn btn-link text-dark"><i class="fa fa-sign-out-alt"></i></button>
        </form>*@
    }
    else
    {
        <a asp-area="Identity" asp-page="/Account/Register" title="Register"><i class="fa fa-user-plus"></i></a>
        <a asp-area="Identity" asp-page="/Account/Login" title="Login"><i class="fa fa-user"></i></a>
    }
